import React from "react";

export default function Expenses() {

    const mainStyle = {
        padding: "1rem 0"
    }

    return (
        <main style={mainStyle}>
            <h2>Expenses</h2>
        </main>
    );
}