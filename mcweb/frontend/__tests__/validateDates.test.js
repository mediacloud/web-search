/* eslint-disable no-undef */
import dayjs from "dayjs";
import { validateDate, earliestAllowedStartDate, latestAllowedEndDate } from "../src/features/search/util/platforms";

test('1 + 1 equals 2', () => {
  expect(1 + 1).toBe(2);
});

